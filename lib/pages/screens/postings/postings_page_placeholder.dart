import 'package:flutter/material.dart';

class PostingsPagePlaceholder extends StatelessWidget {
  const PostingsPagePlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "发布内容区",
        style: TextStyle(fontSize: 20),
      ),
    );
  }
} 